-unless @product.has_auction?
  %h2 Add product to auction

  =form_for [@product, Auction.new] do |f|
    %p
      =f.text_field :value
    %p
      =f.datetime_select :end_at, class: 'date'
    %p
      =f.button class: 'btn btn-primary'